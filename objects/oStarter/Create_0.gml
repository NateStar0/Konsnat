/// @description 

timer = seconds(1);
mx = timer;
compArr =  array_create(array_length(global.colourMap), ((oCamera.colour_state == colour_states.toWhite) ? array_length(global.colourMap) - 1: 0))


with(oMetru) instance_destroy();
